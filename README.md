# 🏥 Medical Appointment Scheduler

A Python-based hospital management system for scheduling patient-doctor appointments with conflict resolution and JSON persistence.

## Features
- Patient & Doctor management
- Smart appointment scheduling with conflict detection  
- JSON data persistence
- Working hours validation
- Appointment filtering and search

## Technologies
- Python 3
- Object-Oriented Programming (OOP)
- JSON for data storage
- Datetime for scheduling

## How to Run
1. Clone the repository
2. Run: `python medical_scheduler.py`

## Project Structure
- `Person`, `Patient`, `Doctor`, `Appointment` classes
- `HospitalSystem` main manager
- JSON persistence layer
- Console-based menu interface
